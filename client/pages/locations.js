import styles from '../styles/locations.module.scss';

const Locations = () => {
    return (
        <div className={styles.wrapper}>
            <h1>LOCATION PAGE</h1>
        </div>
    )
};

export default Locations;