import styles from '../../styles/order_show.module.scss';

const OrderShow = () => {
    return (
        <div className={styles.wrapper}>
            Order Show
        </div>
    )
};

export default OrderShow;
