import styles from '../styles/button.module.scss';

const Button = () => {
    const onClick = () => {
        
    }

    return (
        <button className={styles.button}>
            add to bag
        </button>
    )
}

export default Button;